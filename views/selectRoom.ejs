<!DOCTYPE html>
<html lang="he">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>בחירת חדר</title>
</head>
<body>
  <h1>בחר חדר</h1>
  <form action="/select-room" method="POST">
    <select name="selectedRoom" required>
      <% rooms.forEach(room => { %>
        <option value="<%= room.name %>"><%= room.name %></option>
      <% }) %>
    </select>
    <button type="submit">כניסה לחדר</button>
  </form>
</body>
</html>
